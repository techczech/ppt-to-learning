# Changelog

## Unreleased

- Unified extractor support in CLI with `--engine` selection and related documentation updates.
- Legacy viewer generator script for unified output (`scripts/generate-legacy-viewer.py`).
- Viewer UI: screenshot tools sidebar (generate all/missing/current), per-slide screenshot detection, cache-busting reload, and grid numbering.
- Viewer UI: stats & filters sidebar with slide type counts, SmartArt counts, image/video totals, and filterable grid view.
- Viewer UI: keyboard shortcuts modal (`?`) and new shortcuts for stats/screenshot tools and quick jump.
- Server: unified output compatibility for view/export/slide listing and metadata extraction.
- Screenshots: server endpoint accepts per-slide generation; Python generator supports specific slide numbers.
- Data Repository Path copy button in list and viewer header.

